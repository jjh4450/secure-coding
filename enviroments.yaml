name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - Flask>=3.0
    - Flask-SocketIO>=5.3
    - Flask-WTF>=1.2
    - Flask-Limiter>=3.5
    - Werkzeug>=3.0
    - pytest>=8.0
